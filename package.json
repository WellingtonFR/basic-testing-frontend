{
  "name": "basic-testing-frontend",
  "version": "1.0.0",
  "description": "Use for initial studies in jest and vitest automated tests",
  "main": "app.js",
  "scripts": {
    "test": "vitest --run --reporter verbose",
    "coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "start": "http-server -c-1"
  },
  "keywords": [],
  "author": "Wellington Rocha",
  "license": "GPL-3.0-only",
  "devDependencies": {
    "c8": "^10.1.2",
    "http-server": "^14.1.0",
    "install": "^0.13.0",
    "npm": "^8.6.0",
    "vitest": "^0.9.0"
  }
}
